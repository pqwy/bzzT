inproc {
  akka {
    loglevel = "ERROR"
  }
}

localnetserve {
  include "net"
  akka.remote.server.port = 3001
}

localnetclient {
  include "net"
  akka.remote.server.port = 3002
}

# vim: set sw=2:
